*simplestartify.txt*  A randomly styled start screen for Vim 9
                                                        *simplestartify*

Author:  beamiter
License: MIT

==============================================================================
CONTENTS                                             *simplestartify-contents*

    1. Introduction .............. |simplestartify-introduction|
    2. Requirements .............. |simplestartify-requirements|
    3. Styles and random draws ... |simplestartify-styles|
    4. Dashboard keys ............ |simplestartify-keys|
    5. Recent files .............. |simplestartify-recent|
    5a. Sections ................. |simplestartify-sections|
    6. Options ................... |simplestartify-options|
    7. Commands .................. |simplestartify-commands|
    8. Session safety ............ |simplestartify-sessions|
    9. Compatibility and events . |simplestartify-compatibility|
   10. Plug mappings ............. |simplestartify-mappings|
   11. Highlights ................ |simplestartify-highlights|
   12. Testing and limits ........ |simplestartify-testing|

==============================================================================
1. INTRODUCTION                                  *simplestartify-introduction*

SimpleStartify replaces an empty Vim buffer with a dashboard of recent files,
saved sessions and common actions.  In its default "random" mode, each entry
to the dashboard draws one of four layouts and avoids immediately repeating
the current eligible layout.

The dashboard is a scratch buffer with 'filetype' set to "startify", so rules
written for vim-startify continue to apply.  There is no external program,
daemon or other plugin.

==============================================================================
2. REQUIREMENTS                                  *simplestartify-requirements*

Vim 9.1 or newer.  SimpleStartify is Vim9script only.

Install it with a plugin manager: >

    Plug 'beamiter/simplestartify'
<
or place the repository in a package directory under 'runtimepath'.  There is
nothing to compile for installation.

==============================================================================
3. STYLES AND RANDOM DRAWS                            *simplestartify-styles*

Four layouts are built in, in this order:

    minimal     compact, left-aligned sections          minimum width 16
    boxed       a framed dashboard                      minimum width 28
    centered    centered ASCII banner and entries       minimum width 24
    terminal    a command-line transcript               minimum width 22

The minimum widths are used only to form the random candidate pool.  When no
configured style fits, "minimal" is the safe fallback.  A style explicitly
passed to |:SimpleStartify| is still rendered at narrower widths.  Lines are
measured with |strdisplaywidth()| and shortened with an ellipsis as needed.

The default value of |g:simplestartify_style| is "random".  Each automatic or
manual entry to the dashboard therefore draws from
|g:simplestartify_styles|.  With
|g:simplestartify_avoid_repeat| enabled and at least two eligible styles, the
current style is removed from that draw.  On an active dashboard,
|:SimpleStartifyNextStyle| always uses a random draw, even when a fixed
default style is configured.

|:SimpleStartifyRefresh| updates the data without changing the style.
Resizing reflows the same style from the data already on screen, in every
window showing a dashboard rather than only the focused one, and only when
the width actually changed.  Both operations try to keep the same action
selected.

==============================================================================
4. DASHBOARD KEYS                                      *simplestartify-keys*

    1 ... 9            open the corresponding recent readable file
    displayed letter    load that session, open that bookmark, or run that
                        command, as shown beside the entry
    n                   open a new empty buffer
    r                   draw another random UI style
    q                   close the dashboard window, or quit Vim when it is
                        the only one; see |g:simplestartify_quit_action|

    <CR>                run the selected action
    double left click   select and run an action
    s, CTRL-X           run the selected action in a horizontal split
    v, CTRL-V           run the selected action in a vertical split
    t, CTRL-T           run the selected action in a new tab
    /, CTRL-F           filter the entries as you type
    j, k                next / previous action
    <Tab>, <S-Tab>      next / previous action
    R                   refresh data, keeping style and selection
    d                   confirm and delete the selected session
    D                   forget the selected recent file
    ?, g?               list every live key in a popup

The dashboard buffer keeps its window when an entry is opened in a split or a
tab, so several files can be opened one after another; |<CR>| replaces it.  A
session ignores the split verbs because loading one replaces the whole
layout.  "s", "t" and "v" are never handed out as session shortcuts, so a
session can never silently take one of them over.

FILTERING                                          *simplestartify-filter*

"/" and CTRL-F narrow the dashboard as you type.  Each keystroke re-draws from
the model already in memory - no file is stat'ed and no directory is scanned -
matching the typed text case-insensitively against the entry label and against
the path, session name or command behind it.  A section with no match
disappears for as long as the filter is active.

    <BS>                erase the last character
    <CR>                open the selected entry
    <Esc>, CTRL-C       clear the filter and leave filter mode

Any other key is ignored rather than ending the mode, so a stray arrow key
does not discard what has been typed.  The query is shown in the footer as
well as on the command line, because the command line is gone as soon as
anything else echoes.

Filtering is what makes a list longer than the nine digit shortcuts usable:
entries past the end of a shortcut alphabet are drawn without a "[key]" marker
and are reached with "j"/"k" or by typing.  See |g:simplestartify_recent_count|.

The "?" list is built from the entries the buffer actually has mapped, so it
always names the current session letters.  Those letters replace the
normal-mode command of the same name for as long as the dashboard buffer
exists, which is what "?" exists to make visible.  The footer degrades by
window width and always keeps the pointer to "?".

Recent entries come from two sources, in this order: the files opened during
this Vim session, then |v:oldfiles|.  Vim fills |v:oldfiles| once from
'viminfo' while starting up and never updates it afterwards, so without the
in-session record a dashboard reopened by |:SClose| would list only files from
earlier sessions, and a Vim started with "-i NONE" or an empty 'viminfo' would
list nothing at all.  See |simplestartify-recent|.

Unreadable files and duplicate absolute paths are skipped.  Sessions are
ordered by modification time, newest first.

Opening a recent file may change the window-local directory; see
|g:simplestartify_change_to_vcs_root| and
|g:simplestartify_change_to_dir|.

==============================================================================
5. RECENT FILES                                       *simplestartify-recent*

The recent-files record is a plain text cache, one line per file, formatted as
the epoch time of the last visit, a tab, and an absolute path.  A buffer is
recorded at |BufWinEnter| and |BufWritePost| when it is listed, has an empty
'buftype', is not a preview window, and names a readable file.  The dashboard,
scratch buffers, help, quickfix, terminals and unwritten new files are
therefore never recorded, and neither is anything inside
|g:simplestartify_session_dir|.

Saving re-reads the file and merges it, keeping the newer visit per path, so
several Vim instances sharing one cache do not discard each other's history.
The write goes to a hidden ".simplestartify-tmp-*" neighbour and is renamed
into place, exactly like a session write; a partial write can never replace a
good cache.  A line that does not parse is dropped, so a truncated or
hand-edited cache costs entries rather than raising an error.

This is a cache and it is safe to delete.  Nothing on the dashboard depends on
it existing, and |g:simplestartify_mru_persist| turns it off entirely.

==============================================================================
5a. SECTIONS                                        *simplestartify-sections*

|g:simplestartify_lists| decides which sections the dashboard draws and in
what order.  Seven section types exist:

    files       recent files, anywhere
    dir         recent files below the working directory
    project     recent files below the working directory's Git root, or
                below the working directory when there is no Git root
    sessions    saved sessions from |g:simplestartify_session_dir|
    bookmarks   the paths in |g:simplestartify_bookmarks|
    commands    the commands in |g:simplestartify_commands|
    special     new buffer / restyle / quit

A file already shown by an earlier section is not repeated in a later one, so
listing "dir" before "files" gives "this directory first, then everything
else" without duplicates.  At most a hundred recent paths are examined per
draw, which is far more than the nine that can carry a digit shortcut.

Shortcut keys are handed out from two pools: 1..9 for file, directory and
project entries, and the letters "abcefghilmopuwxyz" for sessions, bookmarks
and commands.  A key pinned in |g:simplestartify_bookmarks| or
|g:simplestartify_commands| is removed from the pools before anything is
assigned, so it can never be handed to a session as well.  When a pool runs
out, the remaining entries are drawn without a marker and are opened with the
cursor and |<CR>|; the "?" list shows them as "<CR> only".

"files", "sessions" and "special" are drawn even when they are empty, showing
"(none yet)".  Every other section is left out when it has nothing in it, so
an unconfigured bookmarks section costs no screen space.
|:SimpleStartifyHealth| names each drawn section with its entry count and
warns about a configured section that was left out for being empty.

==============================================================================
6. OPTIONS                                           *simplestartify-options*

Set options before the plugin loads.  Boolean options accept a Boolean or a
Number.  Invalid values use the stated default.

                                               *g:simplestartify_auto_open*
Open the dashboard at |VimEnter|.  Default 1.  It opens only with no file
arguments, outside Insert mode, when the current buffer is modifiable,
unnamed, unmodified, normal, and contains one empty line.

                                              *g:simplestartify_hide_intro*
Add "I" to 'shortmess' so Vim's intro message does not appear before the
dashboard replaces it.  Defaults to |g:simplestartify_auto_open|, so turning
automatic opening off keeps the ordinary intro screen.  Set it explicitly to
override either way.

                                                   *g:simplestartify_style*
"random" (default), "minimal", "boxed", "centered", or "terminal".  An
unknown string reports an error and draws a random eligible style.

                                                  *g:simplestartify_styles*
Candidate styles for random mode.  Default: >

    ['minimal', 'boxed', 'centered', 'terminal']
<
Unknown and duplicate names are discarded during startup normalization.  If
the normalized list is empty, all four defaults are restored.  At draw time,
styles too wide for the current window (as reported by |winwidth()|) are left
out; if that leaves no candidate, "minimal" is used.

                                          *g:simplestartify_custom_header*
                                          *g:simplestartify_custom_footer*
Replace the banner each layout draws, and the key-hint footer.  Default [].
Each accepts a list of lines, or a string.  A string that looks like a
function call - an identifier, optionally with "#", "." or ":", followed by
parentheses - is evaluated on every draw and may return a string or a list of
strings; any other string is one literal line: >

    let g:simplestartify_custom_header = ['MY VIM', '']
    let g:simplestartify_custom_footer = 'happy hacking'
    let g:simplestartify_custom_header = 'strftime("%A %d %B")'
<
A |Funcref| is deliberately not accepted: Vim refuses to store one in a global
whose name does not begin with a capital (|E704|), so this option could never
hold one.  The evaluated form is the equivalent.

The lines are plain text and each layout renders them in its own style -
centered under "centered", framed under "boxed", comment-prefixed under
"terminal" - so a single configured header still looks like part of whichever
layout was dealt.  Tabs and newlines inside a line are replaced by spaces, and
the usual display-width fitting applies.  An expression that throws reports
"[SimpleStartify] g:simplestartify_custom_header failed: ..." once per draw
and the built-in banner is used instead.

While |simplestartify-filter| is active the footer shows the query regardless
of |g:simplestartify_custom_footer|: it is the reason half the dashboard has
disappeared.

                                             *g:simplestartify_avoid_repeat*
Avoid the current style on the next random draw when the pool has more than
one member.  Default 1.

                                                   *g:simplestartify_lists*
The sections to draw, in order.  Default: >

    [{'type': 'files'}, {'type': 'sessions'}, {'type': 'bookmarks'},
     {'type': 'commands'}, {'type': 'special'}]
<
Each item is a dictionary with a "type" from |simplestartify-sections|, or a
bare type string.  Two optional members: >

    {'type': 'dir', 'header': 'IN THIS DIRECTORY', 'limit': 5}
<
"header" replaces the section's built-in name in every layout and may also be
a list of strings, of which the first non-blank one is used, so a
g:startify_lists value can be reused as is.  "limit" is the maximum number of
entries, overriding |g:simplestartify_recent_count| and
|g:simplestartify_session_count| for that section.  Unknown types and other
value types are discarded during startup normalization; if that leaves
nothing, the default list is restored.

                                               *g:simplestartify_bookmarks*
Paths pinned to the dashboard, in order.  Default [].  Each item is either a
path, or a single-entry dictionary mapping a shortcut key to a path: >

    let g:simplestartify_bookmarks = ['~/.vimrc', {'c': '~/.vim/config'}]
<
The path is expanded with |expand()|.  A bookmark is shown whether or not it
exists yet: opening a directory bookmark opens the directory, and opening a
bookmark to a file that is not there creates an empty buffer for that name.
A requested key must be a single character in [0-9A-Za-z] and must not be one
of the keys the dashboard already owns ("n", "r", "q", "s", "t", "v", "j",
"k", "d", "D", "R", "?"); a rejected key is replaced by an automatic one, and
never silently takes a dashboard key over.

                                                *g:simplestartify_commands*
Ex commands offered on the dashboard, in order.  Default [].  Each item is a
command string, a ["description", "command"] pair, or a single-entry
dictionary mapping a shortcut key to either of those: >

    let g:simplestartify_commands = [
      \ 'SimplePlugUpdate',
      \ ['edit vimrc', 'edit $MYVIMRC'],
      \ {'u': ['update plugins', ':SimplePlugUpdate']}]
<
A leading colon is optional.  Key validation is the same as for bookmarks.
The command runs with the dashboard still open and inside a |try| block: a
command that fails reports "[SimpleStartify] command failed: ..." instead of
throwing out of the mapping.  Session and split verbs do not apply to a
command entry.

                                                *g:simplestartify_skiplist*
Patterns for paths that are never recorded as recent files and never shown.
Each item is a Vim regular expression matched against the absolute path with
|expr-=~#|.  Default: >

    ['\.git[\\/]\%(COMMIT_EDITMSG\|MERGE_MSG\|TAG_EDITMSG\|SQUASH_MSG\)$',
     '\.git[\\/]rebase-\%(merge\|apply\)[\\/]']
<
These are files another tool wrote, not documents, and they are readable, so
without this they take a shortcut slot after every commit.  The list applies
both when a buffer is recorded and when the dashboard is drawn, so adding a
pattern also hides paths recorded earlier and paths from |v:oldfiles|.  An
empty list is honoured and skips nothing; a pattern that does not compile is
dropped during startup normalization rather than throwing on every draw.

                                              *g:simplestartify_recent_count*
Maximum recent files to show.  Default 7, clamped to 0..50.  It is the default
limit of every "files", "dir" and "project" section, and those sections share
one pool of the digit shortcuts 1..9; entries past the ninth are drawn without
a marker and are reached with "j"/"k" or |simplestartify-filter|.

                                             *g:simplestartify_session_count*
Maximum sessions to show.  Default 4, normalized to the range 0..13.  Each
session's non-conflicting shortcut letter is displayed beside it.

                                               *g:simplestartify_session_dir*
Directory containing session files.  Default "~/.vim/session".  See
|simplestartify-sessions| for validation and the trust boundary.

                                       *g:simplestartify_session_persistence*
Automatically rewrite the active managed session at |VimLeavePre| and before
|:SLoad| switches to another session.  Default 0.  This never adopts or writes
a session outside |g:simplestartify_session_dir|.  Explicit |:SClose| always
saves the active managed session regardless of this option.

                                         *g:simplestartify_session_autoload*
Load a "Session.vim" from the working directory when there is no active
session.  Default 0.  It is tried at |VimEnter|, before the dashboard would
open, and on |DirChanged|, so entering a project restores its workspace.

The file is loaded through the ordinary |:SLoad| path, so it cannot skip any
of the guarantees in |simplestartify-sessions|: modified listed buffers refuse
the load, the active managed session is persisted first when that is enabled,
and a session that throws is rolled back.  It is never recorded as the last
session, and because its parent is not |g:simplestartify_session_dir| it is
never rewritten automatically either.

A "Session.vim" is sourced Vimscript.  Turning this option on means every
directory you enter may run code; enable it only for directories you trust.
An active |v:this_session| is never replaced, so this cannot take a workspace
away from you mid-session.

                                         *g:simplestartify_session_savevars*
Global variable names to store in a session, for example >

    let g:simplestartify_session_savevars = ['g:my_project_root']
<
Default [].  Each name must match "g:" followed by an identifier; other items
are discarded during startup normalization.  A name that is unset when the
session is written, or holds a Funcref, job or channel - which |string()|
cannot render as text that sources back - is skipped rather than writing a
session that fails to load.

                                         *g:simplestartify_session_savecmds*
Ex command lines to append to a session verbatim, for example >

    let g:simplestartify_session_savecmds = ['SimpleTreeOpen']
<
Default [].  Both lists are appended to the temporary file after |:mksession|
and before it is renamed into place, so a session carrying extra state is
still replaced atomically.

                                        *g:simplestartify_change_to_vcs_root*
After opening a recent file, use |:lcd| to change to its nearest Git root.
Both a .git directory and a .git file are recognized.  Default 0.

                                              *g:simplestartify_open_action*
What |<CR>| and a double click do with the selected entry: "edit" (default),
"split", "vsplit" or "tabedit".  The "s", "v" and "t" mappings always use
their own verb regardless of this option.  An unknown value uses "edit".

                                              *g:simplestartify_change_to_dir*
After opening a recent file, use |:lcd| to change to its directory.  Default
0.  When both directory options are enabled, a found Git root wins; otherwise
this option supplies the fallback.

                                              *g:simplestartify_quit_action*
What the "quit" entry does: "auto" (default), "quit", "close" or "qall".

    auto    Close the window when the dashboard is one window among several,
            or one tab among several; otherwise leave Vim.
    quit    Always |:quit|.
    close   |:close| the window; with only one window and one tab there is
            nothing to close, so this leaves Vim.
    qall    Always |:qall|.

Leaving Vim is refused, naming the buffers, while a listed buffer is
modified: the entry never discards unsaved work and never raises |E37| from
inside the mapping.  An unknown value uses "auto".

                                          *g:simplestartify_reopen_on_empty*
Reopen the dashboard when the last named buffer is deleted, instead of
leaving Vim on an empty "[No Name]" buffer.  Default 0.

The check runs from a zero-delay |timer_start()| after |BufDelete| and
|BufWipeout|, because those events fire while the buffer is still being taken
apart.  It draws only when no listed buffer has a name and the current window
is unmodified, unnamed, modifiable, of empty 'buftype' and holds one empty
line - the same window conditions as |g:simplestartify_auto_open|.  Nothing
happens without |+timers|.

                                              *g:simplestartify_mru_persist*
Write the in-session recent-file record to |g:simplestartify_mru_file| at
|VimLeavePre| and read it back on first use.  Default 1.  With 0 the record is
kept in memory only, so it still fixes the mid-session case but starts empty
in every new Vim.

                                                  *g:simplestartify_mru_max*
Maximum number of paths kept in that record.  Default 200, normalized to the
range 0..5000.  This is the store size, not the number of dashboard entries;
|g:simplestartify_recent_count| still limits what is displayed.  0 keeps no
paths at all - it is the way to record nothing while leaving |v:oldfiles|
alone, and never means "unbounded".

                                                 *g:simplestartify_mru_file*
Path of the record.  Default "$XDG_STATE_HOME/simplestartify/mru" when
$XDG_STATE_HOME is set, otherwise "~/.vim/simplestartify-mru".  The containing
directory is created with mode 0700 when absent.  Setting this to an empty
string disables persistence in the same way as
|g:simplestartify_mru_persist|.

==============================================================================
7. COMMANDS                                         *simplestartify-commands*

                                                        *:SimpleStartify*
:SimpleStartify [style]
        Open the dashboard.  [style] may be "random" or one of the four names
        in |simplestartify-styles|.  With no argument, use the configured
        style.  A modified buffer is not replaced while 'hidden' is off.

        A window-placement |:command-modifiers| prefix opens the dashboard in
        its own window instead of taking over the current one: >

            :vertical SimpleStartify
            :botright SimpleStartify centered
            :tab SimpleStartify
<
        "aboveleft", "belowright", "botright", "horizontal", "leftabove",
        "rightbelow", "tab", "topleft" and "vertical" are honoured; any other
        modifier is ignored rather than passed on, because "noautocmd" would
        suppress the |FileType| event the dashboard is set up by.  With a
        placement modifier the current buffer is not replaced, so the
        modified-buffer refusal above does not apply, and asking for one from
        inside a dashboard opens a second one.

                                                 *:SimpleStartifyRefresh*
:SimpleStartifyRefresh
        Re-read recent files and sessions, preserving the current style and
        selected action when possible.  Opens the dashboard when run outside
        it.

                                               *:SimpleStartifyNextStyle*
:SimpleStartifyNextStyle
        On the dashboard, make another eligible random draw, preserving the
        selected action when possible.  Repeat avoidance determines whether
        the result must differ.  Outside the dashboard, open it using the
        configured style.

                                                  *:SimpleStartifyHealth*
:SimpleStartifyHealth
        Open a scratch buffer reporting what is and is not working, one
        "[OK]", "[WARN]" or "[ERROR]" line per fact, in four sections:

        ENVIRONMENT     Vim version against the 9.1 minimum, |textprop|
                        support.
        STYLES          |g:simplestartify_style| is a real style name, which
                        styles are eligible at the current width, and whether
                        a configured style is being dropped for being too
                        wide.
        SECTIONS        each section that would be drawn with its entry
                        count, any entries past the end of the shortcut
                        alphabet, and any configured section left out for
                        being empty.
        RECENT FILES    how many entries come from each source, whether
                        'viminfo' can persist |v:oldfiles| at all, and where
                        the recent-file cache lives.
        SESSIONS        the resolved directory with its writability and
                        session count, leftover ".simplestartify-tmp-*" files
                        from an interrupted save, the state of the
                        last-session pointer, and any legacy "__LAST__".

        Every ERROR line names a remedy.  The buffer is 'nofile' and can be
        closed with |:bwipeout|.

                                                   *:SimpleStartifyClean*
:SimpleStartifyClean
        Delete leftover ".simplestartify-tmp-*" files from the session
        directory and report how many were removed.  These are only created
        between writing a session and renaming it into place, so they exist
        only after an interrupted save; no session or pointer file is ever
        touched.

                                      *g:simplestartify_health_last*
The dictionary most recently produced by |:SimpleStartifyHealth|, also
returned by simplestartify#Health() for scripting.  Members: "ok", "report"
(the rendered lines), "styles", "width", "sections" (the model that would be
drawn), "configured" (the section types in |g:simplestartify_lists|),
"session_dir", "session_writable", "session_count", "session_leftovers",
"mru_count", "mru_file", "mru_persist" and "oldfiles_count".

"ok" is false when any check produced an ERROR line, which now includes an
existing but unwritable session directory and an existing but unwritable
directory for |g:simplestartify_mru_file|.  "session_writable" is true only
when the directory already exists and is writable; an absent directory is not
an error, because the first |:SSave| creates it.  When both "mru_count" and
"oldfiles_count" are 0, the recent-files section is empty because nothing has
been recorded yet, not because of a failure.

                                                  *:SimpleStartifyForget*
:SimpleStartifyForget [path]
        Remove [path] from the recent-files record and from |v:oldfiles|, so
        it does not come back on the next draw.  Without [path], forget the
        recent file selected on the dashboard; this is what the "D" mapping
        does.  A path that was not listed reports an error instead of
        silently succeeding.  The removal also survives the cache write at
        |VimLeavePre|, which re-reads the cache before rewriting it: a
        forgotten path is never merged back in, and only opening the file
        again records it once more.  See |simplestartify-recent|.

                                                               *:Startify*
:Startify
        Compatibility alias for |:SimpleStartify| with no argument.

                                                                  *:SSave*
:SSave[!] [name]
        Save a session.  Without [name], prompt for it.  Refuse to replace an
        existing session unless ! is supplied.

                                                                  *:SLoad*
:SLoad[!] [name]
        With [name], load that session.  Without !, refuse when any listed
        buffer is modified; ! permits those buffers to be discarded.

        With neither [name] nor !, prompt with session-name completion.  Bare
        :SLoad! is deliberately different: do not prompt, and restore the
        session recorded by |simplestartify-last-session|.  It reports an
        error when there is no usable last session.  See
        |simplestartify-sessions| before loading any session file.

                                                                *:SDelete*
:SDelete[!] [name]
        Delete a session.  Without [name], prompt with completion.  The
        command refuses deletion unless ! is supplied.

                                                                 *:SClose*
:SClose[!]
        Save the current managed session regardless of
        |g:simplestartify_session_persistence|, clear |v:this_session|, delete
        the listed buffers, and open the dashboard.  Without !, refuse when a
        listed buffer is modified.  A missing, non-regular, unreadable, or
        unwritable managed session, or any save failure, aborts the close.

==============================================================================
8. SESSION SAFETY                                  *simplestartify-sessions*

Session operations fail closed around one configured directory:

- A session name must be a single basename.  Empty names, ".", "..",
  absolute paths, '/', '\', carriage returns, newlines, and names beginning
  with the reserved ".simplestartify-" prefix are rejected.
- The expanded session directory must resolve to an absolute path other than
  the filesystem root.  It is created recursively with mode 0700 when absent
  and must be writable before a save proceeds.
- Atomic session and last-pointer writes allocate a previously nonexistent
  hidden ".simplestartify-tmp-*" path inside the configured directory and
  rename it into place.  Legal session names cannot start with the reserved
  ".simplestartify-" prefix, so they cannot collide with these paths.  Failure
  removes the temporary file and leaves an existing target in place.  A
  crash-leftover temporary remains hidden from the session list.
- The dashboard list accepts only readable regular files.  Every
  ".simplestartify-*" internal file and the legacy "__LAST__" entry are
  excluded, so neither metadata nor migration state consumes a shortcut.
- |:SSave| needs ! to overwrite an existing regular file.  A directory,
  symlink, or other non-file target is refused even with !.  |:SDelete| needs
  ! to delete.  The "d" dashboard mapping asks for interactive confirmation.
- Automatic persistence is disabled by default.  When enabled, it runs at
  |VimLeavePre| and before |:SLoad| switches sessions.  It considers only a
  |v:this_session| whose direct parent is the configured directory; sessions
  elsewhere are ignored.
- |:SClose| is explicit rather than automatic.  It always rewrites the active
  managed session even when |g:simplestartify_session_persistence| is 0.
- Whenever such a rewrite is due, the managed path must still name an
  existing, readable, writable regular file.  If it disappeared, was replaced
  by a directory or symlink, became unreadable/unwritable, or cannot be saved,
  pre-load persistence and |:SClose| report an error and abort rather than
  silently continuing.
- While making a session, "options" is removed from 'sessionoptions'.  The
  original option value and |v:this_session| are restored after a failed save.
- Before a load deletes buffers, the current session layout is snapshotted to
  a temporary session.  If the requested session throws while being sourced,
  that snapshot is sourced and the previous |v:this_session| is restored.  A
  failed persistence write or failed snapshot aborts the load.

A session file is Vimscript.  |:SLoad| sources it; session files therefore
belong to the same trust boundary as vimrc.  Load only files you trust.  The
rollback restores session layout, not arbitrary side effects from hostile
Vimscript that ran before an error.

When automatic persistence is enabled, |:SLoad| first saves the current
managed session and then force-deletes each listed buffer.  |:SClose| always
performs that save before the same explicit listed-buffer delete pass.
Without !, both commands first refuse when a listed buffer is modified.  Their
bang forms explicitly allow those changes to be discarded.  Unlisted buffers
are not included in the pass.

After a successful load the plugin sets |v:this_session| and emits
|simplestartify-session-loaded|.

LAST SESSION                                     *simplestartify-last-session*

After a successful save or load of an ordinary session, SimpleStartify tries
to atomically record the session basename in ".simplestartify-last" inside the
configured directory.  The pointer itself must be a regular readable file;
its contents must pass the same basename validation as a command argument and
ultimately name a readable regular session.  A pointer-update error is
reported without undoing the save or load that already succeeded.

Bare |:SLoad| with ! uses this pointer without opening a prompt, and ! also
permits modified listed buffers to be discarded.  When neither the pointer
nor a supported legacy entry is usable, the command reports that there is no
last session.

For migration, when the new pointer has no usable entry, bare :SLoad! also
recognizes vim-startify's historical "__LAST__".  A regular file is loaded as
the legacy last session.  A symlink is followed only when it resolves to a
readable regular file directly inside the configured session directory; a
successful load then records the target under the new pointer.  "__LAST__" is
always omitted from the dashboard and completion list.

==============================================================================
9. COMPATIBILITY AND EVENTS                 *simplestartify-compatibility*

The dashboard uses 'filetype' "startify".  The commands |:Startify|,
|:SSave|, |:SLoad|, |:SDelete| and |:SClose| preserve vim-startify's familiar
surface, with an added safety bang on |:SClose|.

For every option documented in |simplestartify-options|, the same suffix under
g:startify_ is read when its g:simplestartify_ form is unset.  For example,
g:startify_session_dir is the fallback for
|g:simplestartify_session_dir|.  The new name always takes precedence.

If neither *_auto_open form is set, automatic opening is the inverse of
g:simplestartify_disable_at_vimenter, falling back in turn to
g:startify_disable_at_vimenter.  This preserves the old disable-at-VimEnter
convention.

The plugin sets g:loaded_startify so a later-loaded vim-startify does not add
a second VimEnter hook when both installations remain on 'runtimepath' during
migration.

Bare |:SLoad| with ! implements vim-startify's "load last" behavior without
showing its historical "__LAST__" entry; see |simplestartify-last-session|.

The following |User| events are emitted:

    SimpleStartifyReady             after the dashboard is rendered
                                      *simplestartify-ready*
    Startified                      after the dashboard is rendered
    StartifyReady                   after the dashboard is rendered
    SimpleStartifySessionLoaded     after a session is sourced successfully
                                      *simplestartify-session-loaded*
    SimpleStartifySessionSavePre    before |:mksession| writes the temporary
    SimpleStartifySessionSavePost   after the session is renamed into place
    SimpleStartifySessionLoadPre    before the listed buffers are deleted and
                                    the session is sourced
    SimpleStartifySessionLoadPost   immediately after
                                    SimpleStartifySessionLoaded

The three ready events are emitted in the order shown on every successful
open.  They use <nomodeline>.

The save and load events wrap every write and every load, including the
automatic ones from |g:simplestartify_session_persistence|, |:SClose| and
|g:simplestartify_session_autoload|.  SavePre runs inside the same |try| as
the write, so a hook that throws fails the save and leaves the previous
session file untouched rather than replacing it with half a workspace.

==============================================================================
10. PLUG MAPPINGS                                   *simplestartify-mappings*

<Plug>(simplestartify-open)              *<Plug>(simplestartify-open)*
        Open the dashboard.

<Plug>(simplestartify-refresh)           *<Plug>(simplestartify-refresh)*
        Refresh the dashboard.

<Plug>(simplestartify-next-style)        *<Plug>(simplestartify-next-style)*
        Draw another style.

<Plug>(simplestartify-forget)            *<Plug>(simplestartify-forget)*
        Forget the selected recent file.

<Plug>(simplestartify-help)              *<Plug>(simplestartify-help)*
        Show the dashboard key reference.

No user-facing global key is claimed automatically.

==============================================================================
11. HIGHLIGHTS                                    *simplestartify-highlights*

Override any group after the plugin loads.  Default links are shown in
parentheses.

    SimpleStartifyHeader             generic header                 (Title)
    SimpleStartifyHeaderMinimal      minimal header            (Identifier)
    SimpleStartifyHeaderBoxed        boxed header                   (Title)
    SimpleStartifyHeaderCentered     centered header             (Constant)
    SimpleStartifyHeaderTerminal     terminal header               (String)
    SimpleStartifySection            section heading            (Statement)
    SimpleStartifyEntry              selectable entry              (Normal)
    SimpleStartifyKey                [key] marker                  (Special)
    SimpleStartifyMuted              empty-state text              (Comment)
    SimpleStartifyFooter             help footer                   (Comment)

The links are re-established on |ColorScheme| with |:highlight-default|, so a
colorscheme or vimrc may define any of them explicitly.

Entries are highlighted with |textprop|s when the build has them, which is
what makes SimpleStartifyKey visible: it is a nested span with a higher
priority than the SimpleStartifyEntry span covering the rest of the line.
Without +textprop the same structure is expressed with a |:syn-contained|
match inside a whole-line match, which behaves the same way.

==============================================================================
12. TESTING AND LIMITS                              *simplestartify-testing*

Run the complete local quality gate from the repository root: >

    make check
<
It compiles every Vim9 def and runs the smoke, UI/width, section and filter,
session-safety, recent-files, highlight, health, window-management and
vim-startify compatibility test scripts.

Limits:

- Recent files must still be readable when the dashboard is drawn.
- Only Git (.git directory or file) is recognized as a VCS root, for
  |g:simplestartify_change_to_vcs_root| and for the "project" section.
- At most a hundred recent paths are examined per draw, which bounds the
  stat'ing a "dir" or "project" section can cost.
- Startup normalization clamps recent files to fifty and sessions to thirteen.
  Only the first nine recent entries and the seventeen shortcut letters get a
  marker; the rest are reached with "j"/"k" or |simplestartify-filter|.
- Session files are sourced Vimscript, not a data-only format; see
  |simplestartify-sessions|.

vim:tw=78:ts=8:noet:ft=help:norl:
